# Two Pointers

This section contains interview questions based on the Two Pointers pattern.

**Definition:** The Two Pointers pattern is a technique that uses two indices to traverse an array or string efficiently, often reducing the time complexity from O(n²) to O(n).
